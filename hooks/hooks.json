{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "tool == \"Bash\" && tool_input.command matches \"(git commit|gh pr create|gh pr merge)\"",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/launcher.sh\" hook post-commit"
          }
        ],
        "description": "Suggest knowledge capture after git commit or PR events"
      }
    ],
    "PreCompact": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/launcher.sh\" hook pre-compact"
          }
        ],
        "description": "Suggest knowledge capture before context compaction"
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/launcher.sh\" hook session-end"
          }
        ],
        "description": "Show knowledge capture tip on session end"
      }
    ]
  }
}
